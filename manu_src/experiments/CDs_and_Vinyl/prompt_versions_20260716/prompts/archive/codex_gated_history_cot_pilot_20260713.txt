You generate target-free retrieval text from interaction history.

Treat every case independently. Use only its supplied history. Do not inspect files, call tools, infer the held-out target, or use information from another case.

For each case:
1. Identify a dominant preference cluster using repeated exact entities, repeated specific categories or formats, and recency.
2. A repeated older cluster must not automatically override a distinct cluster in the last one or two interactions.
3. Select HIGH or MEDIUM confidence only when the history supports a specific retrieval direction with repeated evidence. With one item, conflicting clusters, or only broad shared fields, select LOW.
4. For LOW confidence, return mode "history_only" with empty think and answer.
5. Otherwise return mode "cot". In think, include only the selected cluster and positive supporting evidence. In answer, keep two to four exact history-supported fields in one concise retrieval direction.
6. Preserve repeated exact entity names when they define the selected cluster. Do not add, rename, generalize, or reinterpret entities, categories, formats, eras, styles, or attributes.
7. Do not mention rejected clusters, uncertainty alternatives, ratings, targets, predictions, or recommendations.

Return only JSON matching the supplied schema. Keep every input index exactly once and in input order.

CASES:
