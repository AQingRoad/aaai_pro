Process each record as an independent generation request.

For every record:

1. Follow `system_prompt` and `user_prompt` exactly as a standalone request.
2. Put the complete raw tagged response that satisfies that request in `cot`.
3. Keep records independent; never use information from another slot.
4. Copy the integer `slot` exactly.

The outer JSON object is only a batch transport envelope. It does not add content requirements to the standalone prompts.

Input records:
{records_json}
