# Reproducibility

Use this directory for checklists and manifests that connect paper claims to
code, data, configs, checkpoints, and result files.

```text
checklists/   Setup, data, training, evaluation, and submission checks.
manifests/    Artifact manifests for released datasets and reported runs.
```

Small released artifacts remain in `github_artifacts/` while scripts still use
that path as the default.
